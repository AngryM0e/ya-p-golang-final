# Файлы для итогового задания

В директории `tests` находятся тесты для проверки API, которое должно быть реализовано в веб-сервере.

Директория `web` содержит файлы фронтенда.

# TODO List - Веб-приложение для управления задачами
## Описание проекта
TODO List — это веб-приложение для управления задачами (TODO-лист) с возможностью планирования повторяющихся задач. Проект представляет собой бэкенд на Go с веб-интерфейсом, который позволяет:

 📝 Создавать, редактировать и удалять задачи

 🔄 Настраивать повторение задач (ежедневно, еженедельно, ежемесячно, ежегодно)

 📅 Автоматически рассчитывать следующие даты выполнения для повторяющихся задач

 🔐 Защищать доступ паролем

## Проект реализован в рамках итогового проекта по Go и демонстрирует навыки работы с:

 Веб-сервером на Go (net/http)

 Работой с SQLite базой данных

 JWT аутентификацией

## Выполненные задания со звёздочкой ✅
 -**Поддержка TODO_PORT** - сервер использует переменную окружения TODO_PORT для указания порта, на котором он будет слушать.
 -**Поддержка TODO_DBFILE** - приложение использует переменную окружения TODO_DBFILE для указания пути к файлу базы данных SQLite. Только для тестов.
 -**Поддержка TODO_PASSWORD** - приложение использует переменную окружения TODO_PASSWORD для установки пароля, который будет использоваться для аутентификации пользователей.
 -**Реализованы правила повторения** - полная реализация правил повторения задач, включая поддержку всех форматов дат
 -**Переменные окружения** — реализована поддержка TODO_PORT, TODO_DBFILE, TODO_PASSWORD

## Переменные окружения
 **TODO_PORT** = 8080
 **TODO_DBFILE** = scheduler.db
 **TODO_PASSWORD** = 12345678

## Запуск проекта
 ### Без аутентификации
  ```
   go run main.go
  ``` 
 ### C аутентификацией
  ```
  *Windows PowerShell*
   $env:TODO_PASSWORD="12345"
   go run main.go

  *Linux* 
   export TODO_PASSWORD="12345"
   go run main.go
  ```
## Настройки settings.go
 - **Port = 7540** - порт сервера
 - **DBFile = getTestDBPath()** - автоматически вычисляется путь к БД
 - **FullNextDate = true** - включена поддержка полного формата даты
 - **Search = false** - работа с полем поиска задач отключена
 - **Token = generateTestToken()** - автоматически генерируется токен из переменной окружения *TODO_PASSWORD*